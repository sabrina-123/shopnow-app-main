async function loadProducts(){const r=await fetch("/api/products"),ps=await r.json(),c=document.getElementById("products");c.innerHTML=ps.map(p=>`<article class="product-card" data-testid="product-card-${p.id}"><span class="category">${p.category}</span><h3 data-testid="product-${p.id}">${p.name}</h3><p>${p.description}</p><strong class="price">${formatPrice(p.price)}</strong><div class="actions"><a class="btn secondary" href="/product.html?id=${p.id}" data-testid="view-product-${p.id}">Voir</a><button class="btn primary" data-product-id="${p.id}" data-testid="add-to-cart-${p.id}">Ajouter</button></div></article>`).join("");document.querySelectorAll("[data-product-id]").forEach(b=>b.onclick=()=>addToCart(ps.find(p=>p.id===Number(b.dataset.productId))))}loadProducts();