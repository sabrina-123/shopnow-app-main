async function loadProduct(){const id=new URLSearchParams(location.search).get("id"),r=await fetch("/api/products/"+id),c=document.getElementById("product-detail");if(!r.ok){c.innerHTML='<div class="empty">Produit introuvable.</div>';return}const p=await r.json();c.innerHTML=`<div class="detail" data-testid="product-detail-${p.id}"><span class="category">${p.category}</span><h1 data-testid="product-name">${p.name}</h1><p>${p.description}</p><div class="price" data-testid="product-price">${formatPrice(p.price)}</div><button class="btn primary" id="add-detail" data-testid="add-to-cart-${p.id}">Ajouter au panier</button><a class="btn secondary" href="/products.html">Retour</a></div>`;document.getElementById("add-detail").onclick=()=>addToCart(p)}loadProduct();